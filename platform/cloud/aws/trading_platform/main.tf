# Network

# 01. VPC
# 02. Public network: Internet-gateway + public routing tables + public subnets + public routing table associations
# 03. Private network: Elastic IP + private subnets + nat + private routing table + private routing table associations
# 04. Bastion: public network + ssh key + security group
# 05. Broker: allowed security groups + platform subnets group + users
# 06. DB: allowed security groups + platform subnets group + users + config
# 07. Apps: allowed security groups + apps subnets group + auto-scaling groups
# 08. ELB: allowed security groups + frontends subnets group + auto-scaling groups
# 09. DNS: ELB reference, nameservers

# Availability Zones : eu-central-1a, eu-central-1b
